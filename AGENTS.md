# 项目版本约定

以下规则来自用户要求，适用于后续游戏更新。

- 游戏版本以 `content/changelog.json` 的 `version` 为准，格式为 `主版本.次版本.修订号`；当前版本以日志数据为准。
- 小更新（修复、数值平衡、局部表现或交互优化、小规模内容补充）自动将最后一位加 1，例如 0.1.4 → 0.1.5。
- 大更新（新增主要模式、核心玩法系统或大规模内容改版）自动将第二位加 1，同时将最后一位归零，例如 0.1.5 → 0.2.0。
- 第一位只能在用户明确指定时修改，不自动递增。日常的小更新、大更新不需要再次询问版本号。
- 同一次交付包含多项改动时只递增一次；同时包含大小更新时按大更新处理。开发中的修正、测试、重新构建不重复递增。
- 每次游戏更新同步首页版本来源 `content/changelog.json`、新增对应的 `releases` 记录及 README 当前版本说明，保留历史日志。交付构建使用更新后的日志数据。
- 单纯讨论、检查或修改开发约定不提升游戏版本；不要把游戏版本号用于存档或配置的 schemaVersion/configVersion。

- 隐藏终战内容在玩家解锁前不得通过日志、图鉴或战役简报剧透；以持久化的 `falseEndingAchievement` 为解锁依据，新增或修改日志时提供不剧透的 `publicTitle` / `publicItems`。
