# 《战墙》产品需求文档

版本：1.0  
日期：2026-09-05  
交付范围：可完整游玩的单机无限生存游戏，Vue 3 + TypeScript 前端、Python 后端，前后端分离。

本文是 V1 实现与验收依据。标注为“后续”的内容不属于 V1；其余规则均须实现。初始数值是开发基线，集中配置，实际试玩后可以调整，但不得擅自改变核心规则。

## 1. 产品目标与已确认约束

玩家在有重力的二维侧视战场中，从顶部释放正方形建筑，搭建防线、发展经济，抵抗持续增强的怪物，尽可能延长核心存活时间。

已确认约束：

1. 场景使用正方形网格，建筑为正方形。
2. 建筑从场景顶部投放，受重力下落。
3. 投放、落地、失去支撑后的坠落均不导致建筑自身损伤，也不伤害其他友方建筑。
4. 玩家可以拆除建筑，回收部分资源。V1 资源统一为金币，不另设材料。
5. 无备战阶段、无手动开始波次、无最终通关；怪物持续生成并不断增强。
6. 支持普通、疾跑、自爆、飞行、轰炸怪，以及拥有特殊技能和视觉效果的 Boss。
7. 单机游戏允许暂停、保存、继续；不需要账号、联网匹配或在线排行榜。

核心体验：落点选择有意义，结构破坏会改变局势，拆除重建是可行策略，经济投入与即时防守存在取舍。

## 2. V1 范围与完成定义

V1 包含一张完整地图、八种建筑、六种普通/特殊敌人、两种交替出现的 Boss、建筑分支升级、局内强化、存档、设置、历史战绩和基础音画反馈。

必须形成完整闭环：首次启动 → 了解操作 → 新游戏 → 建造和战斗 → 强化及 Boss → 保存退出 → 继续游戏 → 核心被毁 → 战绩结算 → 再次游玩。

不包含：多人、移动端触屏、多人服务器、付费、账号、剧情关卡、地图编辑器、真实刚体旋转、横梁、建筑跨格悬空、地图扩张、局外永久属性成长。不得在 V1 界面放置不可用的占位按钮。

## 3. 目标平台与表现

- 桌面浏览器，键鼠操作；Chrome、Edge 为主要验收环境，Firefox 做基础兼容验证。
- 1280×720 及以上完整适配，推荐 1600×900；低于最低尺寸显示调整窗口提示并暂停模拟。
- 游戏以 Canvas 2D 绘制，Vue 负责菜单、HUD、卡片、对话框和设置。
- 风格：深色工业堡垒、清晰轮廓、少量霓虹功能色。前景与背景分离，敌我容易辨认。
- 建筑和敌人可使用原创程序绘制图形交付，不以缺少商业美术资源阻塞游戏完成。攻击、爆炸、生产、维修和 Boss 技能必须有可区分反馈。
- 不仅依赖颜色传达信息：各类建筑、怪物同时具有不同形状或图标及中文名称。

## 4. 战场、坐标与核心

### 4.1 地图规格

- 逻辑单位：1 格；渲染参考：48 像素/格，实际按可用区域等比缩放。
- 世界宽 36 格，可建造区为 x∈[0,24)，敌人活动及出生区为 x∈[24,36]。
- x 向右增加，y 向上增加；地面位于 y=0。
- 可落稳建筑为 24 列×12 层，列编号 0～23，层编号 0～11。
- 建筑占据单个 1×1 格，落稳后中心为 (列+0.5, 层+0.5)。投放中心高度 y=14.5，世界显示范围覆盖 y=0～16。
- 核心占据列 1、2 的第 0、1 层，即 [1,3]×[0,2]，初始生命 3000，不可移动、拆除或升级。
- 核心顶部可支撑建筑；核心视为友方实体，并遮挡直射弹道。
- 地面不可破坏；无高台、地形洞穴和横梁。摄像机展示完整地图，无镜头移动操作。

### 4.2 初始状态

新游戏：金币 300，空地图加核心，威胁等级 1，模拟时间 0；所有八种基础建筑可购买，无初始建筑。第一个敌人在第 8 秒出现，此后持续进攻；这只是首次上手宽限，不存在可反复进入的备战状态。

## 5. 操作与页面

### 5.1 页面

| 页面 | 内容与行为 |
| --- | --- |
| 主菜单 | 新游戏、继续游戏、历史战绩、设置、操作说明；无有效存档时禁用继续游戏并说明原因 |
| 游戏页 | 顶部金币/核心生命/生存时间/威胁等级/强袭倒计时；底部建筑栏；右侧选中建筑详情；中部战场 |
| 暂停菜单 | 继续、保存并返回主菜单、设置、操作说明 |
| 强化弹窗 | 三选一，显示具体效果，选择后继续 |
| 失败结算 | 生存时间、击杀数、Boss 击杀数、最高威胁、得分、重试、返回主菜单 |
| 历史战绩 | 最近 20 局，按结束时间倒序；另显示本地最高分 |

### 5.2 键鼠操作

- 数字键 1～8 或点击建筑栏选择建筑，进入连续投放模式。
- 鼠标移动显示目标列、预测落点、费用和射界；无效位置显示原因。
- 左键战场投放；一次按下只投放一次，长按不连发；选中类型不自动取消。
- 右键或 Esc 首先取消投放模式；无投放模式时 Esc 切换暂停。
- 非投放模式下左键选中建筑，详情面板可升级或拆除；点击空地取消选择。
- Space 切换暂停；输入框或模态窗口聚焦时不触发游戏快捷键。
- 拆除按钮需按住 1 秒，松开取消；该 1 秒计入游戏模拟时间，暂停期间不推进。显示受影响的上方建筑，无二次确认弹窗。
- 暂停、强化弹窗、失焦暂停时，禁止投放、升级和拆除。
- 浏览器标签隐藏、窗口失去焦点时自动暂停；恢复焦点后由玩家手动继续，不补算离线战斗和收入。
- 设置提供音乐/音效音量、减少动态效果；浏览器允许音频后才开始播放。失去音频能力不影响游玩。

### 5.3 首次引导

首次游戏展示可关闭的操作说明，阅读期间暂停。内容涵盖投放、落点、经济、拆除、暂停；关闭后从时间 0 开始。后续可从菜单重新打开。

## 6. 建造、重力、坍塌与回收

### 6.1 投放

购买成功立即扣款并生成下落建筑，全局投放冷却 1.2 秒。金币不足、冷却未结束、目标列已达最高层、该列已有下落建筑或正在坍塌时禁止投放，且不扣费。

预测位置为目标列当前最高实体顶部；核心占据高度也计入。建筑不得横向移动、旋转、重叠或跨格。单列堆叠支持关系始终竖直，侧面相邻不提供支撑。

下落初速度为 0，重力为 24 格/秒²，速度上限为 18 格/秒。移动必须采用扫掠碰撞，避免穿透地面和建筑，并保证高速经过敌人时不会漏掉砸击判定。

### 6.2 落稳及结构破坏

建筑落到地面、核心或同列建筑顶部后对齐网格，进入工作状态。不模拟弹跳、倾斜和摩擦。

支撑被毁或拆除后，同列失去支撑的建筑从下到上重新计算，下落并依次落稳；实现时保证不重叠、不穿透。建筑在下落中不攻击、不生产、不维修，冷却和生产进度冻结，原有生命与升级保留。

下落不损伤友方；敌人仍可攻击下落建筑，因敌人攻击导致的伤害不属于坠落损伤。满血建筑可能完整落地继续作战，坍塌代价来自停工、射界改变与防线缺口。

### 6.3 砸击

每次建筑由静止转为下落，分配一个独立 fallId；投放也属于一次下落。扫掠路径碰到敌人时造成建筑配置的砸击伤害，同一 fallId 对同一敌人最多一次，可砸中多个敌人。

- 砸击无视护甲，但受护盾影响。
- 砸击不使建筑停下；建筑与建筑、核心、地面才构成落稳表面。
- 建筑落稳后若与存活地面敌人重叠，将敌人移到该建筑右侧最近无重叠位置；若连续占据则继续向右寻找。
- 飞行单位不被建筑阻挡，可受到路径砸击。
- 回收需要时间且损失金币，不增加回收后免费重投的机制。

### 6.4 回收

回收金币 = floor(累计建造及升级费用 × 0.60 × 当前生命/最大生命)。

只可回收已落稳建筑。按住拆除期间建筑正常工作、可受伤；完成瞬间以实际生命计算返还。目标被摧毁则取消拆除，不返还金币。拆除后立即重新计算支撑。没有主动直接搬移建筑的操作。

## 7. 建筑、战斗与经济

### 7.1 通用战斗规则

- 距离以世界格为单位，按实体中心计算；范围边界包含等号。
- 落稳建筑建成时攻击/维修冷却为 0，可立即行动；矿场必须完成完整生产周期后首次产出。
- 默认选取范围内距离核心最近的合法敌人；距离相同按实体 ID 排序。无合法目标不消耗冷却。
- 敌我没有暴击、随机闪避或友军伤害。伤害最低为 1；先扣护盾，再按护甲减免生命伤害。
- 物理伤害乘以 (1-护甲比例)；爆炸、寒冷和砸击无视护甲。护甲不作用于护盾。
- 直射采用即时命中判定和短暂弹道视觉效果，射线被友方建筑/核心及前方敌人阻挡；射击者自身不挡射线。
- 直射武器仅对射线可达敌人开火，实际伤害施加给射线最先命中的敌人。
- 迫击炮为抛射弹，锁定开火时目标位置，0.8 秒后命中；目标离开不会自动跟踪。区域伤害按敌人中心判定。
- 防空塔只对空，忽略友方遮挡；迫击炮只对地，忽略友方遮挡；冰霜塔只对地，范围效果忽略遮挡。
- 地面建筑不需要弹药或电力；除购买、升级外不消耗金币，无负余额、贷款或利息。

### 7.2 基础建筑配置

“间隔”为两次攻击/服务之间的模拟秒数；矿场按完整工作周期结算。

| ID / 名称 | 金币 | 生命 | 能力 | 砸击伤害 |
| --- | ---: | ---: | --- | ---: |
| wall / 城墙 | 40 | 650 | 不攻击，负责阻挡与支撑 | 100 |
| ballista / 弩炮 | 70 | 220 | 物理 24，间隔 0.8 秒，射程 7，只对地、直射 | 35 |
| mortar / 迫击炮 | 120 | 180 | 爆炸 65，间隔 2.8 秒，射程 3～11，爆炸半径 1.3 | 45 |
| anti_air / 防空塔 | 90 | 200 | 物理 22，间隔 0.55 秒，射程 9 | 35 |
| mine / 矿场 | 80 | 160 | 每 10 秒获得 10 金币，无攻击 | 20 |
| repair / 维修站 | 100 | 200 | 每 1 秒修理范围 2.5 内生命比例最低的一个友方 20 生命 | 25 |
| frost / 冰霜塔 | 90 | 200 | 每 1.5 秒对范围 4 内全部地面敌人造成寒冷 5，减速 35%，持续 2 秒 | 30 |
| amplifier / 增幅器 | 130 | 180 | 范围 2.5 内攻击建筑伤害 +20%，不增加减速、维修、生产和砸击效果 | 25 |

维修站可修理核心和其他维修站，不能修理自身；无受损目标时不行动。多个维修站独立工作，溢出治疗丢弃。

多个增幅器对同一建筑只取最强效果，不叠加；多个减速取最强效果并分别记录到期时间。Boss 的减速效果减半。增幅器只在其与目标都落稳时生效。

### 7.3 建筑升级

每座建筑只能选择一次分支，无再次升级或切换。花费即时扣除，升级瞬间生效；最大生命增加时，当前生命按原生命比例换算，不额外免费治满。升级保留已有冷却和生产进度。

| 建筑 | 升级费用 | A 分支 | B 分支 |
| --- | ---: | --- | --- |
| 城墙 | 60 | 加固墙：最大生命 1300 | 尖刺墙：最大生命 850，每次受到近战攻击，对攻击者造成 15 真实伤害 |
| 弩炮 | 100 | 重弩：伤害 55，间隔 1.2 秒，射程 9 | 连弩：伤害 18，间隔 0.3 秒，射程 7 |
| 迫击炮 | 150 | 重炮：伤害 120，爆炸半径 1.8 | 速射炮：间隔 1.5 秒，伤害仍为 65 |
| 防空塔 | 110 | 重型防空：伤害 44，间隔 0.7 秒 | 广域防空：射程 13，其他不变 |
| 矿场 | 120 | 深井矿：每 10 秒产出 18 | 加固矿：最大生命 600，每 10 秒产出 13 |
| 维修站 | 130 | 强效维修：每次修理 40 | 广域维修：范围 4，每次修理 25 |
| 冰霜塔 | 120 | 极寒：减速 50%，持续 2.5 秒 | 冰环：范围 6，伤害 12 |
| 增幅器 | 160 | 强效增幅：伤害加成 35% | 广域增幅：范围 4，伤害加成仍为 20% |

未提及属性继承基础值。真实伤害无视护甲但仍先扣护盾；反伤不触发再次反伤。

### 7.4 收入

- 被动收入：每 5 秒 +5 金币，核心存活且模拟运行才计时。
- 击杀奖励：按敌人配置固定发放，不随生命倍率增加，所有击杀方式归玩家。
- 矿场收入：下落、暂停期间冻结进度；落稳后继续。
- 回收收入：遵循 6.4。
- 金币使用整数，伤害及生命使用浮点运算，界面向上取整显示生命。

## 8. 敌人和寻路

### 8.1 地面移动规则

V1 使用单层地面通道，不做爬墙、跳跃或复杂导航。地面敌人从 x=35.5 出生，向左移动，遇到第 0 层建筑或核心则停止并攻击，障碍消失后继续前进。

普通敌人身体宽 0.6、高 0.8；攻城怪宽 0.9、高 1.1；Boss 单独定义。地面敌人之间可重叠，不互相挤压。停止位置以身体左边缘接触障碍右边缘为准。

地面近战只能攻击挡路的第 0 层建筑或核心，不能隔墙攻击后排；攻击冷却在移动时正常推进，首次接触时冷却已就绪可立即攻击。

下落建筑尚未接触地面时不阻挡地面移动、不成为地面近战目标；它仍可被轰炸和范围爆炸伤害。落稳后立即按其所在层参与障碍判定。

### 8.2 基础敌人配置

| ID / 名称 | 生命 | 速度：格/秒 | 攻击与行为 | 金币 | 首次解锁时间 |
| --- | ---: | ---: | --- | ---: | ---: |
| grunt / 普通怪 | 60 | 0.70 | 近战 12，每 1 秒一次 | 4 | 0 秒 |
| runner / 疾跑怪 | 40 | 1.45 | 近战 8，每 0.65 秒一次 | 4 | 45 秒 |
| bomber / 自爆怪 | 85 | 0.85 | 接触障碍后原地蓄力 1.2 秒，爆炸伤害 150，半径 1.4，自身移除 | 6 | 90 秒 |
| flyer / 飞行怪 | 65 | 1.00 | 出生高度 5，飞向核心上方 (2,3)，到达后每 1 秒对核心造成 10 | 5 | 150 秒 |
| bombardier / 轰炸怪 | 130 | 0.65 | 高度 10，轰炸规则见下 | 9 | 210 秒 |
| siege / 攻城怪 | 320 | 0.40 | 护甲 30%，近战 45，每 1.8 秒一次 | 12 | 120 秒 |

自爆怪蓄力有倒计时闪烁；蓄力期间可被击杀，击杀不触发爆炸。爆炸按建筑或核心 AABB 与圆相交计算，每个实体只受一次伤害；不伤害其他敌人。自行爆炸不算击杀，也不发金币。

飞行怪沿直线接近指定攻击位置，不被建筑阻挡，保持在世界范围内。地面敌人不能被防空塔攻击，空中敌人不能被弩炮、迫击炮和冰霜塔攻击；砸击可攻击双方。

轰炸怪从 x=35.5、y=10 向左飞行；进入可建造区后，每 4 秒选择距自身水平 6 格内累计造价最高的落稳建筑为目标，无目标则向核心移动。选择目标后展示 1.5 秒落点预警，预警结束在锁定列自上而下命中当时最上方实体，造成爆炸伤害 55、半径 0.8；范围每实体一次。若该列已空且不含核心，炸弹落地。到达 x=2 后停留，继续轰炸；造价相同按实体 ID 排序。

### 8.3 数值成长与精英

威胁等级 L=1+floor(生存秒数/60)。敌人出生时固定自身倍率，已出生实体不随等级突然涨血。

- 普通敌人生命倍率：1+0.14×(L-1)。
- 攻击倍率：1+0.10×(L-1)，作用于近战、爆炸、轰炸。
- 移动倍率：min(1.30, 1+0.015×(L-1))。
- L≥6 时，每次普通敌人生成有 12% 概率成为精英；精英生命再乘 1.8，攻击再乘 1.25，金币乘 2。
- 精英随机且等概率获得一种词缀：护盾（护盾为最大生命 30%，不再生）或装甲（护甲增加 20 个百分点，总上限 60%）。
- 外观显示精英轮廓、词缀图标和独立护盾条。Boss 不附加普通精英词缀。

## 9. 无限进攻调度

### 9.1 连续生成

保留“强袭编号”作为连续进攻节奏，不设置波次切换页面。每 60 秒一个周期，以 t mod 60 判断：

| 周期时间 | 阶段 | 生成速率系数 |
| --- | --- | ---: |
| 0～34.999 秒 | 常态 | 1.0 |
| 35～49.999 秒 | 强袭 | 1.6 |
| 50～59.999 秒 | 缓和 | 0.55 |

基础生成速率 r=min(3.0, 0.35+0.045×(L-1)) 个/秒。每步累积 r×阶段系数×dt，达到 1 时消耗 1 并生成一个敌人；可一次生成多个。前 8 秒不累积生成额度。

已解锁敌人按权重选择：普通 45、疾跑 20、自爆 10、攻城 10、飞行 10、轰炸 5。未解锁项不参与，剩余权重重新归一。

战场普通敌人上限 180，Boss 上限 1。达到普通敌人上限时丢弃新生成额度，不在之后集中补刷；生命和攻击仍会随新出生等级提高，不依赖无限实体数量制造难度。

HUD 持续展示距下一次强袭的时间；强袭期间改为展示强袭剩余时间。新敌人解锁前 15 秒显示预警和应对提示；防空塔从开局即可购买。

### 9.2 Boss 调度

从第 300 秒开始，每 300 秒产生一次 Boss 登场请求，按“碎墙巨兽 → 空中母舰”交替。Boss 存活时，新请求只保留一个；Boss 死亡后至少间隔 30 秒再生成积压 Boss，不补发多个。

Boss 存活时普通生成速率额外乘 0.8；当有积压 Boss 请求时改为乘 1.2。Boss 登场前有 10 秒预警，积压登场也必须预警。登场计数仅在实际出生时增加。

Boss 基础生命与伤害应用出生等级倍率；第 n 个实际出生的 Boss 生命再乘 1+0.15×(n-1)。击杀发放 150 金币并触发一次强化选择。

### 9.3 Boss 行为

**碎墙巨兽**：基础生命 2400，护甲 20%，速度 0.35，宽 1.6、高 2；基础近战伤害 70，间隔 1.5 秒。地面移动与障碍判定同普通怪。

技能“破城冲撞”：冷却 12 秒，选择前方 8 格内最近的第 0 层障碍，显示锁定图标和 2 秒蓄力；之后以 4 格/秒向左冲刺，最多 8 格，命中首个障碍造成 300 基础物理伤害并停止，不穿透。无障碍时保持常规行动，技能不消耗。蓄力期间停止普通攻击，冲刺结束后冷却重新开始。

**空中母舰**：基础生命 2000，宽 3、高 1.5，无护甲；从 (35,11) 移动至 (18,11)，速度 0.45，忽略建筑阻挡。

技能“区域轰炸”：到达后每 8 秒选择可建造区内建筑数量最多的连续 3 列，平局取靠核心的一组；若无建筑则选覆盖核心的 3 列。每列展示 2 秒预警，然后分别向当前最上方实体投弹，每枚基础爆炸伤害 100、半径 0.8。不同炸弹可重复伤害同一实体。

技能“护航机群”：到达后每 15 秒在自身位置生成 3 只飞行怪，受普通敌人上限约束，无空间则丢弃；飞行怪使用生成时等级，无精英词缀。

Boss 死亡时移除其尚未执行的技能预警与召唤计时；已生成的敌人和已发出的攻击仍存在。

## 10. 局内强化

每生存满 120 秒触发一次强化，Boss 击杀另加一次。多个触发排队，每次处理一个，游戏模拟暂停。选择完成后若仍有队列则继续展示，否则恢复此前的运行/手动暂停状态。

从未达到叠加上限的强化中无放回随机抽取 3 项；不足 3 项则全部展示。池耗尽时展示唯一选项“应急补给：+100 金币”。随机结果在弹窗生成时保存，读档不得重新抽取。

| ID / 强化 | 每层效果 | 上限 |
| --- | --- | ---: |
| attack / 火力改良 | 所有攻击建筑伤害 +10% | 5 |
| durability / 结构加固 | 所有建筑最大生命 +15%，当前生命同比调整；不含核心 | 5 |
| repair_efficiency / 维修优化 | 维修站修理量 +20% | 3 |
| mining / 高效采矿 | 矿场产量 +20%，单次产量最后向下取整 | 3 |
| impact / 重压投放 | 砸击伤害 +25% | 4 |
| deployment / 快速吊装 | 全局投放冷却 -0.15 秒，最低 0.6 秒 | 4 |
| core_repair / 核心抢修 | 立即回复核心 600 生命，不超过上限 | 不限 |
| subsidy / 战时补贴 | 被动收入每次 +2 金币 | 4 |

同类加成按层数相加，不逐层相乘；不同来源相乘。例如最终建筑伤害=分支基础伤害×(1+0.10×火力层数)×(1+有效增幅器加成)。建筑生命和矿场收入也从基础/分支配置重新派生，避免反复乘算造成漂移。所有强化影响已有与新建建筑。

## 11. 暂停、失败与评分

- 暂停时停止模拟时间、收入、刷怪、下落、技能、弹道和冷却；菜单动画可继续。
- 核心生命≤0 的当步立即进入失败，优先级高于强化选择；不再处理后续战斗奖励。
- 得分=floor(生存秒数×10)+普通及精英击杀数×5+Boss 击杀数×500。
- 结算记录 runId、生存时间、击杀数、Boss 击杀数、最高威胁、得分和结束时间。
- 失败后清除该 runId 的继续游戏存档；写入战绩失败则保存本地待提交记录，允许重试，不阻止重开。
- 新游戏覆盖已有进行中存档前明确提示；这是覆盖确认，常规建造不需要确认。

## 12. 前后端职责与技术方案

### 12.1 前端

使用 Vue 3、TypeScript、Vite、Pinia、Canvas 2D。游戏模拟、碰撞、敌人 AI、随机数、渲染和交互全部运行在浏览器本地；不对每帧或每次攻击调用后端。

- 游戏引擎为独立 TypeScript 模块，不依赖 Vue、DOM 或 Pinia。
- Vue 仅管理 UI 和用户命令；Pinia 保存 UI 状态、当前局摘要和存档请求状态。
- 完整世界状态由引擎持有，避免把所有实体变成 Vue 深层响应式对象。
- Canvas 用 requestAnimationFrame 渲染；模拟采用固定 1/60 秒步长、累计器，单帧最多补算 8 步；超出部分丢弃，不制造瞬间赶帧伤害。
- 一个固定步长内按“命令 → 调度/生成 → 移动及结构 → 建筑效果和攻击 → 敌人攻击/技能 → 伤害与死亡 → 收入及奖励 → 快照事件”执行。实体按 ID 稳定遍历。
- 每次伤害扣除后立即标记死亡，死亡实体不能在本步后续行动；核心死亡立即结束本步战斗。其他删除与支撑重算在死亡阶段处理，并在下步开始下落。
- 使用空间网格查询碰撞、范围目标，禁止每帧对所有实体做无差别两两检测。
- 使用可序列化的确定性伪随机数发生器，固定为 xorshift32；非零 uint32 种子，存储当前状态，禁止模拟代码使用 Math.random()。
- 游戏输入转为命令，在下一模拟步执行，并再次验证金币、目标和冷却。禁止由 UI 直接修改世界数据。

不引入大型物理引擎；规则化竖直堆叠由专用系统实现。V1 不需要 WebSocket、SSR、Web Worker 或 ECS 框架。

### 12.2 后端

使用 Python、FastAPI、Pydantic、Uvicorn，SQLite 通过标准库 sqlite3 访问。后端负责内容配置读取、单槽存档、设置和本地战绩持久化。

- 后端不持续运行战斗，不负责逐帧模拟，也不要求浏览器关闭后继续生产。
- 单机可信客户端，不做反作弊或在线竞技验证；后端仍验证请求类型、字段范围、体积、版本及基础引用关系。
- 默认监听 127.0.0.1:8000；开发前端 127.0.0.1:5173，通过 Vite 代理访问 /api。
- 不开放任意来源 CORS；只允许显式配置的本地前端来源，不携带账号凭据。
- SQLite 文件位于 data/warwall.sqlite3，程序自动建表；不使用 Redis、Docker 或外部数据库作为运行前置条件。
- 前端可在后端暂时断开时继续当前战斗；菜单展示存档服务不可用，不静默声称已保存。

### 12.3 配置单一来源

根目录 content/game-config.json 为数值及静态内容单一来源，包含 configVersion、世界、建筑、升级、敌人、Boss、调度、强化等数据。后端启动校验并通过 API 提供；前端首次启动获取后才允许新游戏，成功后缓存配置用于故障恢复。

前后端代码不重复维护建筑数值表；从后端 OpenAPI 生成前端接口类型，生成产物提交仓库。配置 schema 由后端 Pydantic 定义，前端启动时也验证必要版本和关键字段。

## 13. 项目文件结构

保持一个仓库、两个应用，目录按职责划分，避免每个类一个文件的过度拆分。

```text
战墙/
├── PRD.md
├── README.md
├── .gitignore
├── content/
│   └── game-config.json
├── frontend/
│   ├── package.json
│   ├── package-lock.json
│   ├── index.html
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── src/
│   │   ├── main.ts
│   │   ├── App.vue
│   │   ├── style.css
│   │   ├── api/                 # HTTP 客户端、生成的接口类型
│   │   ├── stores/              # UI/设置/会话状态
│   │   ├── components/          # HUD、建筑栏、详情、弹窗
│   │   ├── views/               # 菜单、游戏、战绩
│   │   └── game/
│   │       ├── engine.ts        # 固定步长、命令入口、生命周期
│   │       ├── types.ts         # 世界、实体、命令、事件
│   │       ├── random.ts        # 确定性随机数
│   │       ├── systems/         # 建造物理、战斗、敌人、调度、经济
│   │       ├── render/          # Canvas、效果、坐标转换
│   │       └── persistence.ts   # 序列化、快照恢复、校验
│   └── tests/                  # 引擎测试及 Playwright 流程
├── backend/
│   ├── pyproject.toml
│   ├── requirements.lock
│   ├── app/
│   │   ├── main.py
│   │   ├── api.py              # 小规模项目路由集中维护
│   │   ├── schemas.py
│   │   ├── config.py
│   │   └── storage.py          # SQLite 表、事务、查询
│   └── tests/
└── data/                       # 运行时生成，不提交数据库与备份
```

组件与系统超过单一职责时再拆分。不得把全游戏逻辑写在 App.vue，也不创建空目录或没有实际用途的抽象层。

## 14. 数据模型、接口和存档

### 14.1 世界快照

快照为显式 JSON DTO，禁止直接序列化 Vue 对象、函数或 Canvas。至少包括：

| 数据组 | 必须保存的内容 |
| --- | --- |
| 元信息 | schemaVersion、configVersion、runId、种子、随机数状态、nextEntityId、revision |
| 进程 | 模拟 tick、金币、最高威胁、击杀与 Boss 计数、进行中状态 |
| 核心 | 当前生命 |
| 建筑 | ID、类型、分支、累计花费、位置、速度、生命、落稳状态、fallId、已砸击敌人 ID、攻击/生产/维修剩余时间 |
| 敌人 | ID、类型、精英词缀、出生倍率、生命/护盾、位置、AI 状态、冷却、减速剩余时间、锁定目标 |
| 战斗 | 未命中的迫击弹、投弹/技能预警、命中剩余时间、伤害及目标位置 |
| 调度 | 刷怪累积值、Boss 请求与预警、登场计数、下一强化触发时间 |
| 经济与强化 | 被动收入剩余时间、强化层数、强化队列和当前候选 |
| 操作 | 投放剩余冷却、有效未执行命令；不保存鼠标位置和按住拆除手势 |

实体派生属性从匹配版本配置恢复。纯粒子、飘字、音效播放进度不保存。加载后总是暂停，点击继续才恢复；若有强化候选则先完成选择。

### 14.2 API 约定

前缀 /api/v1，JSON UTF-8，字段使用 camelCase，日期为 UTC ISO 8601。成功直接返回资源；错误统一为 `{ "error": { "code": "...", "message": "..." } }`。所有 FastAPI 默认验证错误转换为此格式。

| 方法与路径 | 请求 | 成功响应 |
| --- | --- | --- |
| GET /health | 无 | 200：`{status:"ok", apiVersion:1}` |
| GET /config | 无 | 200：完整游戏配置 |
| GET /save | 无 | 200：`{revision, savedAt, snapshot}`；无存档返回 204 |
| PUT /save | `{expectedRevision, snapshot}` | 200：`{revision, savedAt}`；首次 expectedRevision=0 |
| DELETE /save | 查询参数 runId、expectedRevision | 204；只删除匹配的进行中存档 |
| GET /settings | 无 | 200：`{musicVolume, sfxVolume, reducedMotion, tutorialSeen}` |
| PUT /settings | 完整设置对象 | 200：持久化后的完整设置 |
| GET /runs?limit=20 | limit 为 1～100 | 200：`{items:[战绩], bestScore}` |
| POST /runs | `{runId, durationTicks, kills, bossKills, maxThreat}` | 201：完整战绩；同 runId 重试返回 200 原记录 |

服务器根据 durationTicks/60 计算生存时间与得分并生成结束时间，不接受客户端自行提交得分。POST /runs 事务内写战绩并清除相同 runId 存档，不删除其他新游戏存档。

错误状态：422 字段或引用非法、409 revision 冲突或不支持的存档版本、413 快照超过 4 MiB、500 持久化失败。请求客户端显示可理解中文信息，保留恢复机会。

数据库表：save_slot（固定 ID=1、revision、run_id、snapshot_json、saved_at）、settings（固定 ID=1、value_json）、runs（run_id 唯一及战绩字段）。存档更新和版本检查同一事务完成，revision 不因删除归零，空槽也保留当前 revision。所有存档写入、删除和结算清理均递增 revision，清空时保留槽位行并将内容置空。GET /save 无存档时通过 X-Save-Revision 返回版本；首次数据库版本为 0。允许跨源开发地址时须显式暴露此响应头。

设置默认值：musicVolume=0.3、sfxVolume=0.7、reducedMotion=false、tutorialSeen=false；音量范围为 0～1。快照中的 revision 是读取或提交前的基准版本，成功保存后同步为响应版本；它不参与游戏模拟确定性比较。

### 14.3 保存和故障处理

- 每 15 秒模拟时间、强化选择后、页面自动失焦暂停时保存；手动保存并返回菜单必须等待明确结果。
- 前端使用 IndexedDB 保留最新快照与待提交结算；保存顺序为先写本地副本，再请求后端。
- 自动保存请求串行；已有请求在途时合并为最新待保存快照，避免旧快照覆盖新状态。
- 页面关闭事件不作为唯一保存手段。标签切换优先写本地快照，尽力提交后端。
- 后端不可用时允许“保留本地存档并返回”，界面明确显示仅保存在此浏览器。
- 重连后，同 runId 且无 revision 冲突才自动上传；409 时不强行覆盖，暂停并展示本地/服务器存档的时间和进度，让玩家选用一个。
- 新游戏替换存档使用当前 revision；先保留原存档备份，收到保存成功后才确认替换。不能因为初始化失败丢失旧局。
- 配置或 schema 版本不匹配时不加载旧数据；保留原数据，允许导出 JSON，提示新开游戏。V1 不承诺跨规则版本迁移。
- 本地恢复比较同 runId 的模拟 tick；不同 runId 不自动合并或择新覆盖。
- 使用浏览器跨标签通知检测同源重复游戏标签，后打开者暂停并提示切换已有页面；后端 revision 作为最终冲突保护。

## 15. 视觉、音效和交互反馈

- 核心生命≤30% 时显示低血量警示，音效有间隔限制，避免连续轰鸣。
- 建筑被选中显示生命、功能、升级、回收金额与射程；血条在受伤、选中或鼠标悬停时显示。
- 攻击武器使用不同弹道与命中特效；冰霜显示范围脉冲，维修显示连线，矿场显示金币反馈，增幅器显示作用范围。
- 普通爆炸、Boss 蓄力和轰炸预警必须可区分；预警在减少动态效果下仍保留静态范围与倒计时。
- 坍塌使用下落运动和落地尘土表现，不显示建筑坠落伤害数字。
- 同类飘字可合并；粒子总量上限 500，达到上限时丢弃低优先级装饰效果，不丢失技能预警。
- V1 提供可关闭的简单环境音乐、投放、命中、爆炸、金币、升级、警报和失败音效，可使用程序合成；集中音量控制。

## 16. 性能和质量要求

- 目标环境为普通集显笔记本、1920×1080 浏览器视口；在 README 记录实际验收设备与浏览器，不能以未测量结果宣称性能达标。
- 150 座建筑、180 只普通敌人、1 个 Boss 场景下，目标平均 55 FPS 以上，固定步模拟平均耗时小于 8 ms；提供开发用性能面板及固定种子压测场景。
- 无敌人与实体遗留造成的无限内存增长；失败、退出、重新开始时清理 RAF、事件监听、音频实例和计时器。
- 游戏断网/后端中断不导致当前模拟崩溃；数据库写入使用事务，错误可见且可恢复。
- 前端构建、TypeScript 检查和后端测试通过；控制台无未处理异常。
- 数据库、日志、缓存、node_modules、虚拟环境不提交仓库；锁定实际安装验证后的依赖版本。

## 17. 验收标准

### 17.1 引擎自动化测试（Vitest）

1. 建筑从顶部落地和从 10 层坍塌后，生命不因坠落减少；移除下层后上层无重叠地落稳。
2. 满列、金币不足、全局冷却中、同列坍塌中投放均失败且不扣费。
3. 同次下落对同一敌人仅伤害一次；高速下落不会穿透地面；落稳重叠敌人被推至右侧有效位置。
4. 满血、半血、升级建筑回收金额符合公式；拆除期间死亡无回收；拆除手势可取消。
5. 弩炮有遮挡判定；迫击炮有最小射程；防空不打地面；同类减速和增幅不错误叠加。
6. 下落冻结建筑功能，暂停冻结全部模拟计时，恢复无补发收入或离线伤害。
7. 时间推进可解锁全部敌人、触发强袭和两种 Boss；Boss 积压不连续瞬间生成。
8. 相同种子及命令序列产生相同状态；中途保存恢复后继续产生与未中断运行相同的模拟状态。
9. 核心死亡优先于强化；结算仅触发一次；自爆怪自行爆炸无击杀金币。
10. 同时发生支撑死亡、迫击命中、技能到期等边界事件时，无重复奖励或失效目标异常。

### 17.2 后端测试（pytest）

覆盖配置校验、存档读写与空槽版本、非法快照、版本冲突、容量上限、设置持久化、战绩幂等、只清理同 runId 存档、进程重启后恢复数据。

### 17.3 浏览器流程测试（Playwright）

- 新游戏 → 投放城墙/弩炮/矿场 → 选中升级 → 拆除 → 暂停 → 保存退出 → 刷新 → 继续，世界状态正确。
- 强化三选一可选择，前后不会恢复到错误暂停状态。
- 模拟后端不可用后继续作战，保存状态明确；恢复服务后可以保存或处理冲突。
- 核心被毁后出现完整结算，刷新后战绩仍在，同一局不重复计入。
- 1280×720 和 1920×1080 下 HUD、建筑栏、弹窗无裁切，Canvas 点击与视觉格子一致。

### 17.4 实际试玩验收

- 新玩家阅读一次说明后能完成投放、升级和回收。
- 所有建筑、怪物和 Boss 在开发测试入口可单独生成验证；该入口只在开发模式显示。
- 至少完成一次 20 分钟人工试玩和一次 60 分钟无渲染模拟稳定性测试；记录种子与问题，不能用加速模拟替代实际操作体验。
- 默认种子下合理使用城墙、弩炮与防空的基础策略能够进入第一个 Boss 战；完全不建造应自然失败。
- 首轮平衡以“经济发展有风险、至少两种布局能进入第一个 Boss 战、没有明显无限金币循环”为标准，记录测试结论并调整集中配置。

## 18. 实施顺序与交付物

| 阶段 | 必须产出 | 完成门槛 |
| --- | --- | --- |
| 1. 基础工程 | 前后端可启动、配置接口、Canvas 和固定步引擎 | 页面能读取配置，坐标及缩放正确 |
| 2. 可玩闭环 | 核心、投放、重力、城墙/弩炮、普通怪、金币、失败重开 | 不依赖调试按钮即可玩一局 |
| 3. 完整玩法 | 八建筑、升级、回收、六敌人、持续调度、两 Boss、强化 | 内容与本文规则齐全 |
| 4. 持久化与界面 | 菜单、设置、存档、继续、故障恢复、战绩、音画 | 完整用户流程成立 |
| 5. 验证与平衡 | 自动化测试、试玩记录、性能检查、缺陷修复 | 第 17 节达标并记录实际结果 |

最终交付：完整源码、配置、依赖锁文件、自动化测试、README 启动/测试/构建说明、实际验收记录。不能只交付原型截图或依赖调试入口才能玩的演示。

README 必须包含 Windows PowerShell 步骤：创建 Python 虚拟环境并安装锁定依赖，启动 `uvicorn app.main:app --host 127.0.0.1 --port 8000`；前端执行 `npm ci`、`npm run dev`。两个命令分别在 backend 和 frontend 目录运行。

前端必须提供 `dev`、`build`、`preview`、`typecheck`、`test`、`test:e2e`、`generate:api` 脚本；后端提供 `pytest` 测试入口。生产验证采用构建后的前端静态预览，并配置 /api 代理或明确本地 API 地址，不能只验证开发服务器。

## 19. 后续扩展边界

V1 稳定后再评估双侧进攻、横梁与悬空结构、更多地图、建筑移动、不同核心、局外内容解锁和桌面安装包。当前结构必须允许添加配置化内容，但不为未确定功能提前搭建复杂框架。

本版本的实现原则：让连续战斗中的投放、坍塌、经济和改建形成完整体验，优先保证规则一致、操作清楚、数据可恢复、代码可维护。
