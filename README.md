# 战墙 · WARWALL

当前游戏版本：**0.3.15**。首页点击 **更新日志 · v0.3.15** 查看本次更新和此前功能回顾。

更新记录维护在 `content/changelog.json`：发布时修改 `version`，在 `releases` 首部追加版本号、标题与更新内容，保留已有记录。早期改动在 `history` 按主题汇总，不补造版本号或发布日期。

版本规则：小更新自动递增最后一位（如 0.1.4 → 0.1.5）；大更新递增第二位并将最后一位归零（如 0.1.5 → 0.2.0）；第一位由作者决定。每次交付只升级一次版本，并同步更新日志。完整开发约定见 [AGENTS.md](AGENTS.md)。

基于 [PRD.md](PRD.md) 实现的桌面浏览器单机无限生存游戏。Vue 3 + TypeScript + Pinia 管理界面，Canvas 2D 绘制战场，独立固定步引擎运行战斗；FastAPI + SQLite 提供配置、存档、设置与战绩。

## 启动（Windows PowerShell）

安装 Python 3.11+ 与 Node.js 22.12+。本机交付环境已经安装项目依赖并生成生产构建。

第一次安装，在项目根目录执行：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.lock
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

另开 PowerShell，进入项目的 frontend 目录：

```powershell
cd frontend
npm ci
npm run dev
```

访问 http://127.0.0.1:5173。后端必须先启动，首次游戏需要通过 API 获取配置。

验证生产版本：保持后端运行，在 frontend 执行：

```powershell
npm run build
npm run preview
```

访问 http://127.0.0.1:4173。开发和生产预览均代理 `/api` 到 `127.0.0.1:8000`，不依赖跨域许可。

已构建后，也可在根目录运行 `powershell -ExecutionPolicy Bypass -File .\start.ps1`。脚本在后台启动两个服务，控制台按 Enter 后只停止本次启动的进程。不要与已经占用 8000/4173 端口的服务重复启动。

## 操作

- 怪物现在有步伐、旋翼、攻击前扑等动作；轰炸物从机体释放并实体飞行。两个 Boss 新增震波、巨岩、激光与导弹攻击，详见 [怪物战斗更新](docs/enemy-combat-update.md)。
- 新增桥梁（9）、加农炮（0）、狙击塔（-）。桥梁点击格子指定高度，可水平延伸并承载炮塔，失去全部支撑后坠落。详细数值与分支见 [新建筑设计](docs/new-buildings.md)。
- 迫击炮会转向目标并从炮口发射抛物线炮弹，显示尾迹、炮口闪光、后坐和命中冲击；弩炮与防空塔也会朝目标转向。减少动态效果会关闭后坐和尾迹。
- 首页“怪物图鉴”可查看全部六种怪物与两个 Boss 的造型、基础属性、行为和应对建议，可筛选地面、空中与 Boss。
- 设置中的“试听战斗音效”用于检查音量；战斗音效增益已提高，加入动态压缩，并避免重复播放同一视觉特效。
- 数字键 1—8 或点击建筑卡片选择建筑，鼠标显示预测落点，左键每次投放一座。
- 右键 / Esc 取消投放。取消后点击建筑，查看生命、选择一次分支升级，或按住拆除 0.3 秒回收。
- Space 暂停 / 继续；失焦自动暂停，恢复窗口后手动继续。
- 矿场生产金币，城墙阻挡地面敌人，上层火力需要射界；防空塔专门打空中单位。
- 游戏每 15 秒模拟时间自动保存。菜单提供“保存并返回主菜单”，保存失败明确提示浏览器副本状态。

## 测试

后端：

```powershell
cd backend
.\.venv\Scripts\python.exe -m pytest -q
```

前端：

```powershell
cd frontend
npm run typecheck
npm test
npm run build
npx playwright install chromium firefox
# 后端与 npm run preview 都需要运行
npm run test:e2e
```

`npm test` 包含一小时无渲染模拟和两种自动建造策略，结果写入 `docs/stability-result.json`、`docs/balance-result.json`。压测会人工补满核心和敌人，这是稳定性负载，不能当作平衡或人工试玩结果。

`npm run generate:api` 从运行中后端的 OpenAPI 生成 `src/api/generated.ts`。修改 Pydantic schema 后应重新生成并提交类型文件。

## 代码与数据

- `content/game-config.json`：运行时数值与内容单一来源。`build_config.py` 仅用于从初始基线重新生成，调平衡应直接修改 JSON。
- `frontend/src/game/engine.ts`：命令、重力、战斗、经济、调度与确定性 xorshift32 状态。
- `frontend/src/game/systems/spatial.ts`：空间桶与射线 AABB 查询。
- `frontend/src/game/render/`：程序绘图与音效，不参与模拟确定性。
- `frontend/src/game/persistence.ts`：串行保存、IndexedDB 副本与冲突保护。
- `backend/app/`：配置校验、API、Pydantic 快照、SQLite 事务。
- `data/warwall.sqlite3`：首次请求自动创建，可通过 `WARWALL_DB` 环境变量改到测试数据库。不要提交数据库。

开发模式在底部提供敌人 / Boss 生成、强化和结算测试入口，生产构建不显示。性能面板显示实际 RAF 频率和每帧模拟耗时。八类建筑可直接从正常建筑栏验证。

存档是单槽，覆盖新局前提示并保留浏览器备份；读档总是暂停。不同 runId 或 revision 冲突需要显式选择。版本不兼容保留并支持导出 JSON，不自动迁移。

## 弹体、桥梁与背景音更新

箭塔发射带箭头和尾羽的实体箭矢，加农炮发射可见铁弹；按距离计算飞行时间，碰撞后结算伤害。桥梁使用空心钢桁架与连续桥面，升级具有不同外观。

背景音景包含风声、低频运转声与缓慢音符，点击进入游戏后启用，可在设置中调整“背景音景”音量（调至 0 静音）。暂停时渐弱，无需下载音频资源。更新后先保存，再按 Ctrl+F5 刷新页面。

## 验收记录与限制

新增吊装站、拦截阵列、母舰部位、军械箱和高速自爆飞船，具体操作及母舰坠毁规则见 [战术玩法首批试玩](docs/tactics-playtest.md)。新建筑位于建筑栏右侧，可横向滚动查看。

矿场产能衰减、Boss 提前远程起手、事件可见性修复及新事件规则见 [经济与后期压力调整](docs/pressure-update.md)。选择矿场可查看当前实际产出。

困难模式首个 Boss 固定为地面巨兽，母舰最早从第二轮登场；首轮预警存档恢复后同样适用。已登场的 Boss 保持原状。

首次运行会自动打开可跳过的新手引导，分五步介绍核心防守、城墙投放、箭塔部署、分支升级与生存规则。建造和升级使用独立练习区，不消耗金币或修改战局存档。完成或跳过后记住选择；设置 → 新手引导 · 重新体验，或主菜单 / 游戏菜单 → 操作说明 → 重新体验新手引导可再次打开。引导期间战局暂停，返回后按 Space 继续。

新增裂地沙虫、虫巢女王和雷暴巨兽，分别进入第 15、20、25 分钟的 Boss 轮换（前一只未消灭时顺延）。沙虫先预警 5 秒，再从地下贯穿约三列建筑，摧毁路径上的建筑；出土后有 12 秒集火窗口。女王召唤虫群并发射酸液，雷暴巨兽使用预警落雷和等离子轰炸。怪物图鉴可查看新造型与应对方法。

特殊事件增加陨石雨与地底裂隙，和导弹齐射、空降轮换出现，均提前显示落点或出现区域。狙击塔在简单、普通、困难模式均最多同时存在 20 座（下落中也计数），摧毁后释放名额；旧存档或切换难度后超限时保留已有塔，但不能继续建造。

敌人与 Boss 每分钟持续强化，已在场单位也保留剩余生命、护盾比例同步成长，暂停时不增长。以生存分钟数 m 计算：生命倍率为 `1 + 0.12m + 0.018×max(0,m−10)²`，伤害倍率为 `1 + 0.07m + 0.0045×max(0,m−10)²`，无固定等级上限。第 n 次 Boss 登场额外乘以生命 `1 + 0.15(n−1)`、伤害 `1 + 0.08(n−1)`。这些倍率与难度、精英倍率叠加；移速和场上数量仍保留性能及可操作性上限，已发射弹体不会中途改变伤害。

具体执行结果见 [docs/acceptance.md](docs/acceptance.md)。未完成的人工试玩、真实集显设备性能与浏览器覆盖会明确标注；自动化测试通过不等同于 PRD 全部人工验收通过。

技术文档参考：[Vite 官方入门](https://vite.dev/guide/)、[FastAPI 官方入门](https://fastapi.tiangolo.com/tutorial/first-steps/)。

## 战场背景

新增「故事战役 · 黎明计划」：五张地图串联背景故事，逐关解锁防御设施与敌人，每关有剧情简报和专属 Boss。终章同时迎战母舰与虫后，胜利后显示独立结局画面。入口位于主菜单，详情见 [战役模式说明](docs/campaign-mode.md)。

主菜单新增「测试模式：10 分钟后开启左侧战场」。10:00 开放核心左侧 20 列建造区，10:20 左翼开始进攻；通过左右战场按钮切换视角，两线共用核心与金币并持续战斗。完整规则见 [双线测试模式](docs/left-front-test.md)。

Ctrl+A 或「全部接管」按钮可接管全部已落稳攻击塔。地底裂隙现在针对阵地造成破土冲击，裂缝持续 3 秒，虫群从空隙出土。新增精英「地渊孵化兽」：6 分钟后进入刷怪池，预警 6 秒后从建筑底部钻出，首批释放 4 只虫，之后每 12 秒孵化 2 只；生命 1500、护甲 10%、场上最多 2 只，随难度与时间成长。优先摧毁本体可停止增援。

新增 5 个部队槽位（F1—F5 快速接管，Ctrl 追加、Ctrl+Shift 替换）、点击防御塔直接接管、13 章指挥学院，以及设置中的宽阔 / 经典布局切换。操作与验证说明见 [部队与指挥学院](docs/command-academy.md)。

新增空中堡垒与指挥虫协同：堡垒空投增援、坠爆波及地面虫群；指挥虫组织护盾掩护、防空诱饵与自爆飞船集群突击。具体规则见 [协同机制说明](docs/coordination-playtest.md)。

设置 → 战场背景可通过缩略图选择灰烬之城、沉没港湾、黄沙旧都、凛冬禁区和旧日荒原（最初的山丘烟囱背景）。切换立即生效并自动保存，服务版和离线版均支持；旧设置默认使用灰烬之城。背景按显示分辨率缓存，只在切换或窗口尺寸变化时重绘。

矿场经济（0.1.5）：按存活矿场的建造顺序计算递减，超过前 6 座后只降低后建矿场收益，同列惩罚也只作用于后建者。下落中的矿场保留序号但暂停生产；拆除后后续矿场可恢复效率，吊装换列会重新计算同列效率。侧栏显示每分钟原产能、递减损失、实际矿产与固定补贴，投放矿场时显示新矿净增产能。

0.1.6：女王共生虫巢显示虫卵与孵化倒计时，幼虫从巢口出现；虫巢是女王技能的一部分，随女王移动，不是独立可攻击单位。击败女王后停止孵化。拆除建筑按住 0.3 秒，提前松开取消。

0.1.8：护甲虫基础生命 300、护盾 280、物理护甲 15%；无需指挥虫即可掩护 3.2 格内普通地面友军，承担 80% 伤害。反弹消耗原伤害 65% 的护盾并返还 80% 伤害。破盾后掩护和反射失效，不自动回盾。已有存档敌人不补血补盾，新生成敌人使用新基础属性。

0.1.9：指挥虫改为行进整队，取消间距停步、护盾等待与倒退归队；落后单位最多加速 60% 追赶，前排保持推进。建筑阻挡、攻击停留、冰霜减速和自爆飞船限时集结仍生效。

0.1.10：地渊孵化兽基础生命 1500、护甲 10%、周边伤害 100，6 秒破土预警，首批 4 只、后续每 12 秒 2 只；保留出土占位破坏。无限模式沙虫满 15 分钟才加入 Boss 候选池，并非 15:00 强制出场；战役指定 Boss 不受影响。旧存档内已经出现的沙虫不移除，已有孵化兽不重置当前生命和计时。

0.2.0 终章改版：4:00 女王；8:00—10:00 普通刷怪降至 35%，暂停新增事件；10:00 母舰与深渊潮汐领主同时登场。潮汐领主发射深水雷和三重潮汐，半血进入狂潮（施法加快、核心易伤 15%）。全部三名统领被击败且母舰残骸落地后通关。若女王仍存活，允许三 Boss 同场。旧终章存档保留原双 Boss 规则，重开终章体验新版本；其他战役与无限模式不加入潮汐领主。

0.3.0 新终章：10:00 仅母舰登场；残骸实际撞击落地且核心存活后弹出成就“结束了？”。12 秒过场先显示成就，再响起船体碰撞与波涛，领主从背景沉船爬入建筑区中央。此时建筑淡化，战斗、计时和经济冻结，操作锁定；离开窗口自动暂停，可点击继续观看。过场完毕领主位于 (12,7)，巨大上身延伸到画面顶端，且不被建筑遮挡；暴雨闪电持续至本局结束，音量随对应设置。成就可在设置中查看，过场每 2 秒保存检查点。旧终章存档保留旧规则，重开体验新版。

0.3.1：隐藏档案依据已保存的发现成就解锁；滚雷音效与闪电同步，领主重绘为沉船重甲巨兽。鼠标进入建造区、选择/接管/移动/拆除建筑时，领主体透明度降至 22%，血条和攻击预警不受影响。

0.3.2：领主仅对建筑/核心覆盖区域保留 22% 不透明度，空白区域不淡化；血条位于窗口顶端中央。生命固定 12000，旧存档按剩余比例迁移。新增 2.4 秒起步预警的三段巨爪横扫，正常每 12 秒、狂潮每 8 秒；空降每 13 秒 4 名、狂潮每 9 秒 6 名，带 2.5 秒预警，从画面顶部落下，受敌人数量上限约束。

0.3.3：领主生命 = ceil(20 × 120 × 165 / 3.8) = 104211，以基础狙击塔理论持续伤害计算，不含升级/增益/护甲/狂潮易伤，实际击杀时间因战况而异。旧存档按剩余比例迁移。领主到来后本局矿场和被动补给产能 ×3，递减后再乘 3，产能面板和实际到账一致；不影响击杀金币、回收或拾取。

0.3.4：战局内打开设置 → 重新开始 → 确认，保留当前难度、测试模式或战役关卡，重置本局进度；成就、战役解锁和设置保留，原战局保留浏览器备份。

0.3.5：所有难度、无限/战役/测试模式下，玩家子弹、箭矢、炮弹与冰霜攻击穿透全部友方建筑，旧存档直接生效；城墙仍挡住地面敌人，敌方与反弹弹体继续命中建筑。

0.3.6：地渊孵化兽基础生命 1500 → 750；体型 2.6×2.8 → 1.8×2 格，模型、碰撞及破土直接摧毁范围同步缩小。已有存档个体保留当前血量，新生成个体使用新血量。

0.3.7：地渊孵化兽基础生命恢复 1500，保留小体型。前 10 分钟随机刷怪权重 ×0.25，10—15 分钟线性恢复，15 分钟后正常；无限/战役均适用，不改变自身孵化小兵间隔。新血量用于之后生成的个体。

0.3.8：战役最后一关矿场和基础补给产能从开局 ×1.5；与领主到来的 ×3 相乘后为 ×4.5，实际收益与面板一致。金币支持半枚收益保存，其他关卡和无限模式不受影响。

0.3.9：终章到达 8:00 时经济产能再 ×2，开局总倍率 ×1.5 → 8 分钟起 ×3 → 领主到来后 ×9，矿场与基础补给同步生效，按存档时间计算且不重复叠加，其他关卡和无限模式不受影响。

0.3.10：空中堡垒坠落基础伤害 320 → 180（约降低 44%），半径保持 4 格，保留地面虫群友伤。已生成残骸保留生成时伤害，之后坠毁使用新基础值。

0.3.11：空中堡垒坠爆对建筑和核心增加 50% 减伤（基础 90），对地面虫群保留基础 180；保留难度/成长倍率、范围、军械箱伤害与其他攻击。已有残骸撞击时同样应用建筑减伤。

0.3.12：终战大血条移至战场上方中央；9:00、9:30、9:45 及最后 10 秒强调最后攻势。新触发过场在母舰实际落地后静候 4 秒，再展示 9 秒成就，之后继续沉船爬出；以 -600 至 720 的过场帧保存，新负值区段为新增停顿，旧进度保留。闪电持续 1.4 秒、带分叉与光晕，滚雷约 10 秒多层低频起伏。

0.3.13：领主首次实际登场时 +1000 G；建筑投放和吊装站搬运冷却减半，正在进行的相关冷却也减半。倍率依据已保存的 storm 状态，不重复领奖；已经进入领主战的旧存档获得后续吊装加速，不补发金币。

0.3.14：新增诱饵投影塔，建筑栏最右侧（快捷键 D），220 G、280 HP、限 1 座（下落中也计数），战役第三关起解锁。首次落稳立即投影，之后每 20 秒一次、持续 6 秒；投影在同侧入侵区（右翼 x=29 / 左翼 x=-26），诱导 12 格内普通敌人的移动与攻击，无实体且无法被打坏。Boss、堡垒、孵化兽及空降中的敌人免疫；墙体会阻断地面诱导路线。每条升级 160 G：延长至 9 秒或间隔缩至 14 秒。发生器被拆除、摧毁或失去支撑时投影失效；存档保留投影和冷却。
